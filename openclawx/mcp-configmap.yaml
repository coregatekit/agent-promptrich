apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-mcp-config
  namespace: openclaw
data:
  openclaw.json: |
    {
      "mcpServers": {
        "tradingview": {
          "command": "uvx",
          "args": ["tradingview-mcp"]
        }
      }
    }